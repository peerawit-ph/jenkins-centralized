pipeline {
    agent any

    stages {
        stage('PoC') {
            steps {
                cleanWs()
                echo "${env.GIT_URL}"
                echo "${env.RJPP_SCM_URL}"
                echo "${env.RJPP_JENKINSFILE}"
                echo "${env.RJPP_BRANCH}"
                echo "${env.RJPP_LOCAL_MARKER}"
            }
        }
    }
}
